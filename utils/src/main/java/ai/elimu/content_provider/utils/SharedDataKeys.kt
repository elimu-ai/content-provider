package ai.elimu.content_provider.utils

/**
 * A collection of keys, which represent data managed by ContentProvider, and could be shared
 * with external apps via SharedDataProvider
 */
object SharedDataKeys {
    const val KEY_LANGUAGE = "language"
}